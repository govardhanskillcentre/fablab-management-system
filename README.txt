FabLab Management System

Local: pip install -r requirements.txt, then python app.py and open http://127.0.0.1:5000
Render Start Command: gunicorn app:app
Login: Firebase Authentication (manage users in Firebase Console > Authentication > Users).
